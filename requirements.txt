# --- Interface Web --------------------------------------------------------
streamlit>=1.30.0       # Support de use_container_width=True utilisé dans app.py

# --- Calcul Scientifique & Traitement d'Image -----------------------------
numpy>=1.24.0
opencv-python>=4.8.0    # Décodage et filtrage spatial (boxFilter, GaussianBlur, medianBlur)
scikit-image>=0.21.0    # random_noise (gestion rng/seed), calcul de PSNR et SSIM
matplotlib>=3.7.0       # Visualisation des spectres de Fourier 2D
pandas>=2.0.0           # Tableau de comparaison et stylage (pandas.Styler)

# --- Imagerie Médicale & Échantillons -------------------------------------
pydicom>=2.4.0          # Parsing DICOM (.dcm), conversion HU et fenêtrage clinique
pooch>=1.8.0            # Requis par skimage.data.brain() pour le stockage en cache
kagglehub>=0.2.0        # Téléchargement de COVID-19 Radiography Database

# --- Tests & Assurance Qualité --------------------------------------------
pytest>=8.0.0
pytest-cov>=4.1.0